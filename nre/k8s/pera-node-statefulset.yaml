---
apiVersion: v1
kind: Service
metadata:
  name: pera-node
  namespace: rpc-mainnet
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: pera-node
  type: ClusterIP
---
allowVolumeExpansion: true
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: pd-ssd
parameters:
  type: pd-ssd
provisioner: kubernetes.io/gce-pd
reclaimPolicy: Delete
volumeBindingMode: Immediate
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: pera-node
  name: pera-node
  namespace: rpc-mainnet
spec:
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: pera-node
  serviceName: pera-node
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "9184"
        prometheus.io/scrape: "true"
      labels:
        app: pera-node
    spec:
      containers:
      - command:
        - /opt/pera/bin/pera-node
        - --config-path
        - /opt/pera/config/pera-node.yaml
        env:
        - name: RUST_BACKTRACE
          value: "1"
        - name: RUST_LOG
          value: info,pera_core=debug,pera_network=debug,narwhal=info,consensus=debug,jsonrpsee=error
        image: x
        imagePullPolicy: Always
        name: pera-node
        ports:
        - containerPort: 9000
          protocol: TCP
        resources:
          requests:
            cpu: "32"
            memory: 128G
        volumeMounts:
        - mountPath: /opt/pera/db
          name: ssd-storage
        - mountPath: /opt/pera/config/pera-node.yaml
          name: pera-config-volume
          subPath: pera-node.yaml
        - mountPath: /opt/pera/config/genesis.blob
          name: pera-genesis-volume
          subPath: genesis.blob
      restartPolicy: Always
      terminationGracePeriodSeconds: 180
      volumes:
      - configMap:
          defaultMode: 420
          name: pera-config
        name: pera-config-volume
      - configMap:
          defaultMode: 420
          name: pera-genesis
        name: pera-genesis-volume
  volumeClaimTemplates:
  - apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: ssd-storage
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 5000Gi
      storageClassName: pd-ssd
      volumeMode: Filesystem
