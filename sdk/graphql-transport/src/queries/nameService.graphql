# Copyright (c) Mysten Labs, Inc.
# SPDX-License-Identifier: BSD-3-Clause-Clear

query resolveNameServiceAddress($domain: String!) {
	resolveIkansAddress(domain: $domain) {
		address
	}
}

query resolveNameServiceNames($address: IkaAddress!, $limit: Int, $cursor: String) {
	address(address: $address) {
		ikansRegistrations(first: $limit, after: $cursor) {
			pageInfo {
				hasNextPage
				endCursor
			}
			nodes {
				domain
			}
		}
	}
}
